/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./*.{html,css,js}",   // Includes all HTML, CSS, and JS files in the root directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

